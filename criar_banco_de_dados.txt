-- Criação do banco de dados
CREATE DATABASE IF NOT EXISTS setembro_amarelo DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
USE setembro_amarelo;

-- Criação da tabela de inscrições (atualizada)
CREATE TABLE IF NOT EXISTS inscricoes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    nascimento DATE NOT NULL,
    email VARCHAR(100) NOT NULL,
    telefone VARCHAR(20) NOT NULL,
    bairro VARCHAR(100) NOT NULL,
    aluno_estacio ENUM('sim', 'nao') NOT NULL,
    indicacao ENUM('sim', 'nao') NOT NULL,
    nome_indicador VARCHAR(100),
    matricula_indicador VARCHAR(50),
    comorbidade VARCHAR(100) NOT NULL,
    genero ENUM('female', 'male', 'others') NOT NULL,
    criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Criação da tabela de usuários (admin)
CREATE TABLE IF NOT EXISTS users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(100) NOT NULL,
    password VARCHAR(255) NOT NULL,
    criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Inserção de um usuário admin (senha: admin123)
INSERT INTO users (username, password) VALUES
('admin', '$2y$10$qCoWr6TUuArzUicRMGF2wuv0q4/0lfHkIaPuqrSpIxGO9jd6rOegS');
